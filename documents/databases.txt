A database is an organized system for storing, managing, and retrieving information. Databases are commonly used by computer programs to store data. Relational databases organize information into tables, rows, and columns, while non-relational databases can store documents and key-value pairs. Programs use database queries to search, retrieve, and update information. Good database systems make it easier to manage large amounts of data efficiently.